# MongoRepository

Generic MongoDB repository adapter.

```ts
import { MongoRepository } from '@haskou/ddd-kernel/adapters/db/mongo';
```

Provide a `MongoDocumentMapper` to keep persistence documents out of the domain.
